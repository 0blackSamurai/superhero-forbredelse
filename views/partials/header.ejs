<!DOCTYPE html>
<html lang="no">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= typeof description !== 'undefined' ? description : 'Utforsk superhelter med detaljert informasjon, bilder og krefter.' %>">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Skip to main content for screen readers -->
    <a href="#main-content" class="skip-link">Hopp til hovedinnhold</a>
    
    <header class="header" role="banner">
        <h1>🦸‍♂️ Superhero Explorer 🦸‍♀️</h1>
        <nav class="nav-links" role="navigation" aria-label="Hovednavigasjon">
            <a href="/">🏠 Hjem</a>
            <% if (!isAuthenticated) { %>
                <a href="/login">Logg inn</a>
                <a href="/register">Registrer</a>
            <% } else { %>
                <a href="/profile">Profil</a>
                <% if (isAdmin) { %>
                    <a href="/admin/users">Admin</a>
                <% } %>
                <a href="/logout">Logg ut</a>
            <% } %>
        </nav>
    </header>

    <main id="main-content" class="container" role="main">
